#app.py#
#This project is built using OLLAMA i.e open source model, streamlit to provide a basic UI interface to actually give it a chatbot like interface, where we can chat actually to a bot.
#Put the codes in app.py
#Run streamlit run app.py
#Then check the interface over here: http://localhost:8501/

#app2.py#
Here the same thing is done as app.py but with paid source llm i.e OPEN ROUTER AI, with API key

#groq.py#
is basically a test to check the groq ai API key.

#openRouter.py#
is Basically, a test to check the Open Router API key.